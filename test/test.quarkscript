charm foo 1; // Creates a new variable named foo and assigns it value of 1
up foo 3; // Increments foo by 3
strange out foo; // Outputs to console

charm bar;
strange in bar; // Input

strange out bar;